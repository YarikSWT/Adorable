# Base44 — Account

`/account` возвращает 404. Account-настройки base44 не выделены в отдельный URL (см. `../settings/notes.md`).

Для Adorable: документируем в `mockups/desktop/settings.html` как одну из sub-секций (Profile = личные данные, Account = email/password/2FA/sessions).
